name: Region - East US 2 EUAP

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  minimal:
    secrets: inherit
    uses: ./.github/workflows/workload-minimal.yml
    with:
      location: eastus2euap
