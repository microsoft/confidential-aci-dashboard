name: Region - Australia East

permissions:
  id-token: write
  contents: read

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:
  pull_request:
    paths:
      - .github/workflows/region-australiaeast.yml

jobs:
  minimal:
    secrets: inherit
    uses: ./.github/workflows/workload-minimal.yml
    with:
      location: australiaeast

  attestation:
    secrets: inherit
    uses: ./.github/workflows/workload-attestation.yml
    with:
      location: australiaeast
